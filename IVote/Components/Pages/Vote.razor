@page "/vote"
@rendermode InteractiveServer
@inject PollService pollService;

@foreach (var Poll in PollList)
{
  <PollFormat poll="Poll" />

}

@code {
  List<Poll> PollList => pollService.GetallPoll();


}